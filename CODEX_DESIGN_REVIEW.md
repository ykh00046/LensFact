# 결론

MVP의 1차 표면은 Concept A Package Decoder가 가장 적합하다. `docs/MVP_BRIEF.md`의 제품 정의가 "contact-lens package에 인쇄된 숫자를 한국어로 번역하고 출처와 한계를 보여준다"는 것이고, 5초 안에 "렌즈 포장 숫자를 설명한다"는 점이 보여야 하므로, 패키지 라벨을 중심에 둔 A가 문제와 해결을 가장 즉시 연결한다. B Evidence Desk는 신뢰를 만드는 핵심 구성요소로 통합해야 하지만 첫 화면 전체가 비교표가 되면 첫 방문자가 "왜 이 서비스를 쓰는지"를 이해하기 전에 데이터 테이블을 만나게 된다. C Material Atlas는 좋은 학습 레이어지만, MVP 홈의 주 표면이 되면 "포장 사양 해석"보다 교육 콘텐츠 서비스로 오인될 위험이 크다.

권장 IA는 `Home / decoder`를 A형 패키지 해석 화면으로 시작하고, 같은 화면 안에 B의 출처 칩, 검증일, 미확인/충돌 상태, "차이 있는 항목만 보기"를 축소 통합하는 방식이다. `Compare official specs`는 B를 별도 2차 화면으로 확장한다. `How to read a package`와 `Lens myths`에는 C의 소재 설명, Dk와 Dk/t 구분, 함수율 오해 해설을 가져오되, 제품 선택을 유도하는 내러티브가 아니라 "사양을 읽는 법"으로 제한한다.

현재 HTML은 시각 콘셉트 보드이지 생산 코드가 아니다. `support.js`, `x-dc`, `sc-for`, `DCLogic`, 인라인 스타일, 고정 크기 캔버스, 샘플 데이터가 섞여 있으며, 예시값은 검증된 사실로 취급하면 안 된다. 구현은 별도 정적 데이터 스키마와 접근 가능한 HTML 컴포넌트로 다시 만들어야 한다.

# 유지할 것

- A의 패키지 라벨 중심 구성은 유지한다. BC, DIA, 소재, 함수율, Dk/t, 교체주기, UV 표기를 한 라벨 안에서 탭하면 해설 패널이 갱신되는 구조는 정적 MVP와 잘 맞고, 20개 제품이어도 데이터 양이 과하지 않다.
- B의 "값마다 출처 유형과 확인일을 붙이는" 표현은 반드시 유지한다. MVP brief의 필수 신뢰 신호인 source type, `verifiedAt`, unknown, conflict 표시와 직접 맞는다.
- B의 미확인 값과 출처 충돌을 약점이 아니라 정보 상태로 보이게 하는 방향은 좋다. 특히 Dk/t처럼 제조사 자료와 허가정보가 다를 수 있는 항목에는 이 패턴이 필요하다.
- C의 소재 설명은 유지하되, 홈 주 표면이 아니라 문맥 도움말, "How to read a package", "Lens myths"의 카드/섹션으로 넣는 편이 맞다.
- 전체 톤은 병원식 권위보다 문서/라벨/근거의 느낌이 강하고, 가짜 의사 이미지나 구매 CTA가 없어서 제품 제약과 잘 맞는다.
- 44px 안팎의 모바일 버튼 높이, `prefers-reduced-motion` 대응, 한국어 우선 폰트 방향은 유지할 만하다.

# 수정 필수

- "선택한 조건과 일치하는 제품 12개"는 추천처럼 읽힐 수 있다. MVP에서는 "추천", "적합", "나에게 맞는" 표현을 피하고 "입력한 포장 표기와 같은/가까운 공개 사양"처럼 중립 필터 언어로 바꿔야 한다. 결과 순서도 랭킹이 아니라 가나다순, 브랜드명순, 검증일순 같은 명시적 정렬이어야 한다.
- Dk와 Dk/t는 UI와 데이터 모두에서 분리해야 한다. 현재 Concept C의 범위 막대와 예시 문장은 소재군별 Dk/t 범위처럼 보이는데, 실제 제품별 두께, 도수, 측정 조건, 단위가 다르면 단순 비교가 위험하다. 각 값에는 `metricType: Dk | Dk/t`, 단위, 측정 조건, 두께 기준, 도수/테스트 파워, 원문 라벨을 붙여야 한다.
- Dk/t 숫자에는 단위를 비워 두지 말아야 한다. 소비자용 표시는 단순화할 수 있지만, 원자료 단위나 제조사 표기 원문을 보존하고 "조건 미공개 시 직접 비교 제한" 문구를 값 옆에 붙여야 한다.
- 함수율은 bulk/core/surface 여부를 스키마와 UI에 넣어야 한다. brief가 이 구분을 요구하므로, 그냥 `%` 값 하나만 보여주면 나중에 서로 다른 기준의 값을 같은 항목처럼 비교하게 된다.
- UV는 "있음"만으로는 부족하다. 공식 자료에 공개된 경우에도 "UV 차단 표기 있음", "차단율/표준/조건 공개 여부", "선글라스 대체 아님"의 경계가 필요하다. 표기가 없다는 것과 기능이 없다는 것이 다르다는 현재 문구는 유지한다.
- 교체주기는 "최대 사용 기간" 또는 "허가/표기된 교체 일정"으로 표현해야 한다. "1일"을 안전한 재사용 판단으로 읽히지 않게, 일회용 재사용 금지 오해와 분리된 고정 안내가 필요하다.
- 출처 정체성을 더 구체화해야 한다. 현재 출처 칩은 "식약처 허가정보", "제조사 공식 사양" 수준인데 MVP에는 URL, 문서명/페이지 제목, 원문 발췌 위치, `verifiedAt`, 값별 source id가 필요하다. "2026.08.27 확인"은 프로토타입 표시일 뿐 실제 검증일로 이식하면 안 된다.
- 제품 매칭은 국내 판매명, 제조사명, 수입/판매원명, 허가명, 영문명, 패키지 표기가 1:1로 맞지 않을 수 있다는 경고와 별칭 구조가 필요하다. 사용자가 포장명으로 찾을 때 잘못된 제품에 연결되는 것이 가장 큰 신뢰 리스크다.
- Concept C의 소재군 범위와 인사이트는 실제 데이터가 충분히 검증되기 전까지 일반화하지 말아야 한다. "실리콘 하이드로겔은 산소 지표가 높다" 같은 설명도 편집 해설로 분리하고, 개별 제품의 착용감/안전/적합성 예측으로 이어지지 않게 해야 한다.
- 모바일 비교 화면은 2열 제품 비교를 390px에 고정하면 긴 한국어 제품명, 출처명, 충돌 주석이 쉽게 잘린다. 모바일에서는 2열 표보다 제품 A/B 탭, 항목별 접힘 카드, 또는 가로 스와이프가 아닌 세로 비교 패턴을 요구해야 한다.
- 클릭 가능한 행이 `div onClick` 형태인 프로토타입 구조는 생산에서 금지한다. 행 선택, 소재 전환, 필터 토글은 `button` 또는 라디오/세그먼트 컨트롤로 만들고 키보드 포커스, `aria-pressed`, 포커스 표시를 제공해야 한다.

# 구현 리스크

- 정적 MVP로 구현 가능하다. 20개 제품, 7개 주요 사양, 10개 myth check는 JSON/TS 정적 데이터와 정적 라우팅만으로 충분하다. 단, 값 하나가 곧 문자열 하나가 아니라 provenance와 상태를 가진 객체여야 한다.
- 최소 데이터 구조는 제품 기본정보, 표기명 별칭, 사양 필드, 값 원문, 정규화 값, 단위, source type, source URL, `verifiedAt`, measurement qualifiers, unknown/conflict status, editorial note를 분리해야 한다. 충돌은 단일 값 덮어쓰기 대신 여러 source value를 보관하고 UI에서 펼칠 수 있어야 한다.
- A의 라벨-해설 구조는 구현 난도가 낮지만, 실제 20개 제품으로 확장할 때 "제품 선택", "사용자가 직접 숫자 입력", "선택한 제품의 공식 사양 보기"가 한 화면에 섞이면 복잡해진다. MVP 1차 흐름은 제품 선택형 또는 사양 입력형 중 하나를 기본값으로 정해야 한다.
- B는 데스크톱에서는 강하지만 모바일에서 가장 위험하다. 긴 제품명, 값 상태, 출처 칩, 확인일, 주석이 동시에 표시되므로 정보 밀도 조절 규칙이 없으면 접근성과 가독성이 무너진다.
- C는 콘텐츠 검수 비용이 가장 크다. 소재 설명은 과학적으로 그럴듯해 보일수록 의료/성능 주장으로 오해될 수 있어, 모든 문장에 편집 해설 라벨과 한계 문구가 필요하다.
- 디자인 보드는 고정 1440/390 프레임을 축소해 보여주는 방식이라 실제 반응형 검증이 아니다. 생산에서는 360, 390, 768, 1280px 이상에서 텍스트 넘침, sticky 영역, 하단 시트, 비교 카드의 스크롤 소유권을 별도로 검증해야 한다.
- 외부 Google Fonts 의존성은 포터빌리티와 성능 리스크다. 정적 MVP는 폰트 자체 호스팅 또는 시스템 폰트 fallback 전략을 정하고, 한국어 폰트 용량과 `font-display`를 관리해야 한다.
- `oklch()` 색상은 최신 브라우저에서는 유효하지만, 지원 범위와 fallback을 정해야 한다. 생산 CSS에는 디자인 토큰과 fallback 색상을 둬야 한다.
- 접근성 리스크가 있다. 현재 보드는 콘셉트라서 `aria-label`, 명확한 heading hierarchy, 스킵 링크, 키보드 조작, 포커스 스타일, 색상에 의존하지 않는 상태 표시가 부족하다. 생산에서는 source/conflict/unknown을 색만으로 구분하면 안 된다.
- 법적/안전 경계는 대체로 지켜졌지만, "조건과 일치", 소재별 산소/함수율 범위, UV 있음, 교체주기 같은 표현이 실제 데이터와 결합될 때 suitability recommendation처럼 읽힐 수 있다. 모든 결과 화면에 "최종 착용 적합성, 도수, 임상 판단은 안경원 또는 안과 전문가에게 확인" 경계를 반복 노출해야 한다.

# 디자이너에게 보낼 피드백

1. MVP 주 표면은 Concept A로 확정하고, 첫 화면은 패키지 숫자 해석에 집중해 주세요. B는 비교/근거 레이어로 통합하고 C는 학습 보조 섹션으로 축소하는 방향이 구현과 제품 정의에 가장 맞습니다.
2. "선택한 조건과 일치하는 제품" 문구를 추천처럼 보이지 않는 중립 필터 언어로 바꿔 주세요. "추천", "적합", "베스트", "순위"로 읽힐 여지가 있는 카운트/정렬/강조는 제거해야 합니다.
3. 값 표시 컴포넌트의 필수 상태를 디자인에 명시해 주세요: 공식 확인, 미확인, 출처 충돌, 측정 조건 미공개, 편집 해설. 색상뿐 아니라 텍스트/아이콘/레이블로도 구분되어야 합니다.
4. Dk와 Dk/t, 함수율 기준, UV 표기, 교체주기의 한계 문구를 각 항목 옆에 들어갈 실제 UI 패턴으로 보여 주세요. 특히 Dk/t는 단위와 측정 조건이 없을 때 비교 제한 상태가 필요합니다.
5. 모바일 비교 화면을 다시 설계해 주세요. 390px 2열 비교는 긴 한국어 제품명과 출처 주석에 취약하므로 항목별 카드, 제품 전환 탭, 접힘 상세 등으로 재구성하는 것이 좋습니다.
6. 실제 출처 UI에는 source URL, 출처 유형, 문서명 또는 원문 위치, 확인일이 들어갈 자리를 확보해 주세요. 현재 칩은 방향성은 좋지만 실제 provenance를 담기에는 너무 작습니다.
7. 샘플 값은 모두 프로토타입 데이터임을 더 강하게 표시해 주세요. 디자인 보드의 수치, 소재 범위, 제품 수 카운트를 실제 검증값처럼 보이게 하면 구현 단계에서 오해가 생깁니다.
8. 생산 핸드오프에서는 고정 캔버스가 아니라 실제 반응형 규칙을 주세요. 하단 시트 높이, sticky header, 긴 값/긴 출처명, unknown/conflict 주석이 들어왔을 때의 줄바꿈과 스크롤 동작이 필요합니다.
9. 구매, 예약, 쿠폰, 제휴 링크, 의학적 적합성 판단으로 이어질 수 있는 CTA는 계속 배제해 주세요. CTA는 "포장 숫자 입력", "공식 사양 비교", "출처 보기"까지만 허용하는 편이 안전합니다.
10. Concept C의 교육 문장은 "일반 설명"과 "개별 제품 사실"을 시각적으로 더 분리해 주세요. 소재 설명이 착용감, 안전성, 산소 공급 우수성 주장으로 보이지 않게 편집 해설 라벨과 한계 문구를 고정 패턴화해야 합니다.
